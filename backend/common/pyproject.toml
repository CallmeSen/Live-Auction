[build-system]
requires = ["setuptools>=80"]
build-backend = "setuptools.build_meta"

[project]
name = "live-auction-common"
version = "0.1.0"
requires-python = ">=3.13"

[tool.setuptools.packages.find]
include = ["auction_common*"]
