import { useState } from 'react';
import { mockCategories } from '../../../mocks/users';

export default function AdminCategoriesPage() {
  const [categories, setCategories] = useState(mockCategories); const [name, setName] = useState('');
  const add = (event: React.FormEvent) => { event.preventDefault(); if (!name.trim()) return; setCategories((current) => [...current, { id: `cat-${Date.now()}`, name: name.trim(), slug: name.trim().toLowerCase().replace(/\s+/g, '-'), items: 0, status: 'ACTIVE' }]); setName(''); };
  return <div className="mx-auto max-w-5xl px-6 py-10 sm:py-14"><span className="font-mono-tag text-xs uppercase tracking-[0.2em] text-[var(--color-primary)]">Admin · Danh mục</span><h1 className="mt-2 font-display text-4xl">Quản lý danh mục</h1><form onSubmit={add} className="mt-7 flex gap-3"><input value={name} onChange={(event) => setName(event.target.value)} placeholder="Tên danh mục mới" className="min-w-0 flex-1 rounded-lg border border-[var(--color-border)] bg-[var(--color-surface)] px-4 py-3 text-sm outline-none focus:border-[var(--color-primary)]" /><button className="rounded-md bg-[var(--color-primary)] px-5 py-3 text-sm font-semibold text-[var(--color-bg)]">Thêm danh mục</button></form><div className="mt-6 overflow-hidden rounded-xl border border-[var(--color-border)] bg-[var(--color-surface)]">{categories.map((category) => <div key={category.id} className="grid grid-cols-[1fr_100px_90px_auto] items-center gap-4 border-b border-[var(--color-border)] p-4 last:border-0"><div><p className="text-sm">{category.name}</p><p className="mt-1 font-mono-tag text-[10px] text-[var(--color-text-dim)]">/{category.slug}</p></div><span className="text-xs text-[var(--color-text-muted)]">{category.items} vật phẩm</span><span className="text-xs text-[var(--color-success)]">{category.status}</span><button onClick={() => setCategories((items) => items.filter((item) => item.id !== category.id))} className="text-xs text-[var(--color-danger)]">Xóa</button></div>)}</div></div>;
}
