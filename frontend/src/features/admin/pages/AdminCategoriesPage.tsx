import { useState } from 'react';
import { mockCategories } from '../../../mocks/users';

export default function AdminCategoriesPage() {
  const [categories, setCategories] = useState(mockCategories); const [name, setName] = useState('');
  const add = (event: React.FormEvent) => { event.preventDefault(); if (!name.trim()) return; setCategories((current) => [...current, { id: `cat-${Date.now()}`, name: name.trim(), slug: name.trim().toLowerCase().replace(/\s+/g, '-'), items: 0, status: 'ACTIVE' }]); setName(''); };
  return <div className="mx-auto max-w-5xl px-6 py-10 sm:py-14"><span className="font-mono-tag text-xs uppercase tracking-[0.2em] text-[#C9A227]">Admin · Danh mục</span><h1 className="mt-2 font-display text-4xl">Quản lý danh mục</h1><form onSubmit={add} className="mt-7 flex gap-3"><input value={name} onChange={(event) => setName(event.target.value)} placeholder="Tên danh mục mới" className="min-w-0 flex-1 rounded-lg border border-[#2a3f31] bg-[#14231a] px-4 py-3 text-sm outline-none focus:border-[#C9A227]" /><button className="rounded-md bg-[#C9A227] px-5 py-3 text-sm font-semibold text-[#0F1B14]">Thêm danh mục</button></form><div className="mt-6 overflow-hidden rounded-xl border border-[#2a3f31] bg-[#14231a]">{categories.map((category) => <div key={category.id} className="grid grid-cols-[1fr_100px_90px_auto] items-center gap-4 border-b border-[#2a3f31] p-4 last:border-0"><div><p className="text-sm">{category.name}</p><p className="mt-1 font-mono-tag text-[10px] text-[#607468]">/{category.slug}</p></div><span className="text-xs text-[#7d9186]">{category.items} vật phẩm</span><span className="text-xs text-[#8fc99c]">{category.status}</span><button onClick={() => setCategories((items) => items.filter((item) => item.id !== category.id))} className="text-xs text-[#ff9a86]">Xóa</button></div>)}</div></div>;
}
